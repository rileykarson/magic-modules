# Terraform Boilerplate
provider "google" {
  project = "${local.project}"
}

resource "random_pet" "prefix" {
  length = 2
}


locals {
  name_prefix = "${random_pet.prefix.id}"

  # Enter your GCP project here!
  project = “YOUR_PROJECT_HERE”
}

# <%= ctx[:name] -%> Configuration

<%= ctx[:content] %>